{
  "watch": ["src"],
  "ext": "ts",
  "ignore": [],
  "exec": "tsc && hb-service stop && hb-service rebuild && hb-service start -I -D && hb-service logs",
  "signal": "SIGTERM",
  "env": {
    "NODE_OPTIONS": "--trace-warnings"
  }
}
